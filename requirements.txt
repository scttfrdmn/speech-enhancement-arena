# Core
torch>=2.0.0
torchaudio>=2.0.0
numpy

# Inference server
fastapi
uvicorn[standard]
python-multipart

# Spectrogram visualization
matplotlib

# For Trainium (install separately on Trn instances):
#   torch-neuronx, torch-xla, libneuronxla  (via Neuron SDK DLAMI)
#   pip install "trnfft[neuron]"            (FFT/complex ops via NKI)
